#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    double res  = 0;
    res = a * 0.2 + b * 0.1 + c * 0.2 + d * 0.5;
    printf("%.1f", res);
    return 0;
}